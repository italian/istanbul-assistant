{
  "name": "istanbul-assistant",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "echo 'DEVCONTAINER_OK' && if [ -f requirements.txt ]; then pip3 install -r requirements.txt; fi",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  }
}
